@* Phân trang *@
<style>
    .paginate {
        display: flex;
        justify-content: space-between;
        font-weight: 500;
    }

    .pagePicked {
        width: 50px;
        height: 38px;
        margin: 0 10px;
    }

    .paginate_sub, .sub-link {
        margin: auto 0;
    }

    .paginate_sub-link {
        display: flex;
        justify-content: flex-end;
    }

    input[type=text], select {
        font-weight: 500;
        border: 1px solid;
        border-color: #eee;
        border-radius: 5px;
    }

    input[type=text] {
        padding: 0 16px;
    }

    .paginate .nav-link {
        height: 34px;
    }

    .pagination {
        margin-top: -9px;
    }

    .page-link {
        color: #515253;
        padding: 8px 18px;
    }

        .page-link:hover {
            color: #515253;
            opacity: 0.8;
            cursor: pointer;
        }

    .infoStudent {
        position: relative;
    }

    .info {
        position: absolute;
        margin-left: 70px;
        display: inline;
    }
</style>